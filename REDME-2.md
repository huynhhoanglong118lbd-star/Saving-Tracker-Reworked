# Savings Tracker PWA

## Deploy lên GitHub Pages (miễn phí, 5 phút)

1. Vào https://github.com → Đăng nhập (hoặc tạo tài khoản)
2. Nhấn **New repository** → đặt tên ví dụ `savings-tracker`
3. Tick **Add a README file** → nhấn **Create repository**
4. Nhấn **Add file → Upload files**
5. Kéo thả toàn bộ 5 file vào: `index.html`, `manifest.json`, `sw.js`, `icon-192.png`, `apple-touch-icon.png`
6. Nhấn **Commit changes**
7. Vào **Settings → Pages → Branch: main → Save**
8. Sau 1-2 phút, app sẽ chạy tại: `https://[username].github.io/savings-tracker`

## Cài lên iPhone

1. Mở link trên Safari (không phải Chrome)
2. Nhấn nút Share (□↑) ở thanh dưới
3. Chọn **"Thêm vào Màn hình chính"**
4. Nhấn **Thêm** → App xuất hiện ngoài màn hình như app thật

## Tính năng
- Nhập `+500000` hoặc `-200000` rồi nhấn OK / Enter
- Có thể thêm ghi chú: `+500000 lương tháng 5`
- Nút nhập nhanh: +100k, +200k, +500k, +1M, -50k, -100k
- Dữ liệu lưu offline trong thiết bị
- Hoạt động không cần mạng sau lần đầu tải
